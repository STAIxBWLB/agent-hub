node_modules/
dist/
*.log
.DS_Store
.env
.env.*
.agenthub/state/
.agenthub/*.db*
.agenthub/checkpoint.md
# this checkout's own hub config: personal paths (key file, Access files)
.agenthub/config.json
# this checkout's own hub files (a project that uses the hub commits its routing.toml; the tool's repo does not)
/.agenthub/
.qwen/
.pi-*/
